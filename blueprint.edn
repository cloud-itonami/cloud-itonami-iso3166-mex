{:itonami.blueprint/id "cloud-itonami-iso3166-mex"
 :itonami.blueprint/name "Independent Public-Sector Market-Entry & Procurement Compliance Service — Mexico"
 :itonami.blueprint/iso3166 "MEX"
 :itonami.blueprint/domain :public-sector/market-entry-compliance
 :itonami.blueprint/license "AGPL-3.0-or-later"
 :itonami.blueprint/social-impact [:usmca-reciprocity-clarity :public-spend-transparency :cross-border-friction-reduction]
 :itonami.blueprint/governor :market-entry-compliance-governor
 :itonami.blueprint/robotics false
 :itonami.blueprint/required-technologies [:identity :forms :dmn :bpmn :audit-ledger]
 :itonami.blueprint/status :public-oss
 :itonami.blueprint/maturity :implemented
 :itonami.blueprint/implemented-slice
 "MarketEntry-LLM Advisor ⊣ Market-Entry Compliance Governor, a REAL
 compiled `kotoba-lang/langgraph` StateGraph (`:intake -> :advise ->
 :govern -> :decide -+-> :commit / :request-approval -> :commit /
 :hold`), append-only audit ledger. Seven governor checks, in priority
 order, ALL HARD violations a human approver cannot override:
 spec-basis citation, evidence-checklist completeness, Mexico-resident
 authorized-representative verification (`:requires-mx-entity?` /
 `:has-mx-entity?`, the flagship check for this jurisdiction),
 independent engagement-fee recompute (base + months x rate), RFC
 verification (`:requires-rfc?` / `:rfc-verified?`), double-draft
 guard, double-submit guard, plus a SOFT confidence-floor/actuation
 gate. `:filing/draft` and `:filing/submit` (real CompraNet
 actuation) never auto-commit in any phase; `interrupt-before
 #{:request-approval}` pauses escalated requests for real human
 sign-off via a checkpointer, and BOTH `:commit` and `:hold` durably
 append to the real audit ledger (`store/append-ledger!`). Store has
 a `MemStore` and a `DatomicStore` (via `kotoba-lang/langchain-store`,
 no hand-rolled EDN-blob codec), proven equivalent by a shared
 store-contract test. 33 tests / 106 assertions green, clj-kondo 0
 errors."}
